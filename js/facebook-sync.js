// Facebook sync disabled.
// The automatic Facebook sync was removed to avoid exposing access tokens in the public repo.
// If you later want to implement secure sync, create a server-side endpoint (Netlify Function / Vercel Serverless / backend)
// that fetches posts using a token stored in environment variables and returns sanitized results to the client.

console.log('facebook-sync disabled');
